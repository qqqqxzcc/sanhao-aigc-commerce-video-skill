interface:
  display_name: "Douyin Video Replication"
  short_description: "Replicate Douyin ecommerce videos, generate storyboard frames, and prepare Seedance or Gemini Omni video prompts"
  default_prompt: "Use $douyin-video-replication to analyze this benchmark video, replace only the product, enforce product fidelity/no deformation, generate actual storyboard images with Codex image generation, QC them cell by cell, and produce Seedance-ready ecommerce video prompts. If I have configured my own Seedance/Ark API key, optionally submit the final video task through the API."
policy:
  allow_implicit_invocation: true